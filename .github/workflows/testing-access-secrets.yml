name: Testing access secrets from script

on:
  workflow_dispatch:
  
jobs:
  testing_case:
    name: Testing access secrets from script
    runs-on: ubuntu-latest
    steps:
      - name: testing access secrets
        run: |
          cat
