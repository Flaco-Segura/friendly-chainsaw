name: Demonstration
on:
  workflow_dispatch:
    # Inputs the workflow accepts.
    inputs:
      site_names:
        # Friendly description to be shown in the UI instead of 'name'
        description: "Site Names with naming convention 'sitename-abbvie-com' and comma separated"
        # Input has to be provided for the workflow to run
        required: true

jobs:
  prepare-site-names:
    name: Prepare site names
    runs-on: ubuntu-latest
    outputs:
      site_names: ${{ steps.prepare-site-names.outputs.site_names }}
    steps:
      - name: prepare-site-names
        id: prepare-site-names
        run: |
          echo "::set-output name=site_names::{\"site_name\":[\"a\", \"b\", \"c\"]}"

  check-site-names:
    needs: prepare-site-names
    runs-on: ubuntu-latest
    strategy:
      matrix:
       site_name: [dlserver-net, alc-dlserver-net, immunology-dlserver-net, marketingtraining-dlserver-net, neuroscience-dlserver-net, oncology-dlserver-net, specialty-dlserver-net, hepatology-dlserver-net, immunology2-dlserver-net, patientoutreach-dlserver-net, usmedical-dlserver-net, womenshealth-dlserver-net, marketaccess-dlserver-net, businesstools-dlserver-net, homeoffice-dlserver-net, speakerprograms-dlserver-net, a3-abvhostingservices-com, allianceforendo-com, ibdperspectives-com, whatsyoursorespot-com, mappinghepc-com, crohnscolitispr-com, ahorroscontigo-com, skyrizi-nl, reumanizar-es, crohn-colitis-info-at, acne-inversa-at, hepatitcsizyasam-com, psodisk-it, sedefleyasam-com, duodopakademi-se, dontturnyourbackonit-com-au, dryeye-com-au, productinformatie-abbvie-nl]
    
    steps:
    - run: |
        echo ${{ matrix.site_name }}
